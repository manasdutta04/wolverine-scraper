{
  "name": "wolverine-scraper",
  "version": "0.1.1",
  "private": true,
  "description": "Self-healing price and stock tracker for niche electronics stores.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "scrape": "node pipeline/run.js",
    "check": "node heal/check.js",
    "heal": "node heal/check.js --fix",
    "web:dev": "npm run dev --prefix web",
    "web:build": "npm run build --prefix web"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0"
  }
}
